version: 0.0
os: linux
files:
  - source: /
    destination: /var/api/
hooks:
  AfterInstall:
    - location: Scripts/npm-install.sh
      runas: root
      timeout: 300
  ApplicationStart:
    - location: Scripts/npm-start.sh
      runas: root
      timeout: 60

